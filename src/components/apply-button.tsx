'use client';

// This component is no longer in use.
// The application logic has been moved directly into the `src/app/jobs/[id]/page.tsx` file
// to simplify the flow and fix rendering issues.

export default function ApplyButton() {
  return null;
}
